# Optional override example - NOT active by default.
#
# Copy to `compose.override.yml` (auto-merged by `docker compose`) or use with
#   docker compose -f docker-compose.yml -f compose.override.yml up -d
# to integrate Pirapire into an existing stack (e.g. behind a reverse proxy) or
# to change ports/volumes without editing docker-compose.yml.
#
# Everything below is commented on purpose. Uncomment only what you need.

# services:
#   pirapire_app:
#     # Attach Pirapire to an existing external network (e.g. a reverse proxy).
#     networks:
#       - web
#     # Do not publish the port directly when a reverse proxy handles TLS.
#     # ports: []
#     labels:
#       - "traefik.enable=true"
#       - "traefik.http.routers.pirapire.rule=Host(`pirapire.example.com`)"
#       - "traefik.http.services.pirapire.loadbalancer.server.port=8000"
#     # Absolute volumes if you prefer a fixed data path:
#     # volumes:
#     #   - /srv/pirapire/data:/app/data
#     #   - /srv/pirapire/logs:/app/logs

# networks:
#   web:
#     external: true
